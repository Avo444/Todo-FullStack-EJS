<nav>
    <div class="container">
        <div class="flex-between">
            <a href="/" class="logo">Todo App</a>

            <ul class="flex">
                <% if(!session.id) { %>
                    <li><a href="/login" class="<%=title === "Login" ? "text-primary active" : "text-primary" %>">Login</a></li>
                    <li><a href="/signup" class="<%=title === "Signup" ? "text-primary active" : "text-primary" %>">Signup</a></li>
                <% } else { %>
                    <li><a href="/logout" class="text-primary">Logout</a></li>
                <% }%>
            </ul>
        </div>
    </div>
</nav>
